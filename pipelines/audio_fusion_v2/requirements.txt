# Pinned for sealed audio-only + fusion-v2 reproduce (robot Macro F1 bit-match).
# Use a clean venv on another machine:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
# scipy pulled by sklearn; pin a wheel-friendly range for py3.10–3.13
scipy>=1.11,<1.18
pytest>=8.0
