import os,json,numpy as np,pandas as pd
from pathlib import Path
from sklearn.linear_model import LogisticRegression
from sklearn.pipeline import make_pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import accuracy_score,f1_score,precision_score,recall_score,classification_report,confusion_matrix
import run_avr_group_selection as avr
import run_multimodal_val_locked_suite as suite
import train_audio_specimen_contact_lift_select_final_test as sl
import train_audio_specimen_contact_consensus_select_final_test as sp
import train_audio_group_consistency_pair_blend_select_final_test as ga
import train_audio_lift_source_blend_select_final_test as lift
import train_val_select_final_test as audio_base
ROOT=Path('/home/ttung05/Desktop/tree_base/tree_structures'); OUT=Path('outputs/audio_feature_benchmarks/spec_lift_visual_group_selection'); IMG=Path('outputs/image_timm_features/vit_base_patch16_clip_224.openai_ft_in1k')
def seg(s): return s.astype(str).str.replace(r'_window_\d+.*$','',regex=True)
def specimen(s): return seg(s).str.replace(r'_segment_.*$','',regex=True)
def main():
 lock=json.load(open(OUT/'selection_lock.json')); b=lock['selected_candidate']; C=float(b['C']); iw=float(b['image_material_weight']); th=float(b['contact_threshold']); audio_base.configure_feature_set('total240')
 h=audio_base.load_manifest(ROOT/'audio_visual_dataset_default/dataset.csv','hand_train'); hf=suite.load_manifest(ROOT/'audio_visual_dataset_default/dataset.csv',ROOT/'audio_visual_dataset_default','hand_train'); yh=hf.y.to_numpy(np.int64); hs=specimen(hf.audio_file).to_numpy(); u,hi=np.unique(hs,return_inverse=True); x=np.load(IMG/'hand_train_full/X.npy').astype(np.float32); sx=np.vstack([x[hi==i].mean(0) for i in range(len(u))]); sy=np.array([yh[hi==i][0] for i in range(len(u))]); model=make_pipeline(StandardScaler(),LogisticRegression(C=C,max_iter=1200,class_weight='balanced',random_state=42)); model.fit(sx[sy>0],sy[sy>0]-1)
 test=audio_base.load_manifest(ROOT/'audio_visual_dataset_robo_default/dataset.csv','robot_test'); tf=suite.load_manifest(ROOT/'audio_visual_dataset_robo_default/dataset.csv',ROOT/'audio_visual_dataset_robo_default','robot_test'); y=tf.y.to_numpy(np.int64); xt=np.load(IMG/'robot_test/X.npy').astype(np.float32); ts=specimen(tf.audio_file).to_numpy(); tu,ti=np.unique(ts,return_inverse=True); tx=np.vstack([xt[ti==i].mean(0) for i in range(len(tu))]); raw=pd.read_csv(ROOT/'audio_visual_dataset_robo_default/dataset.csv'); hi=pd.read_csv('outputs/audio_feature_benchmarks/audio_highsr_temporal_tta_select/reports/audio_highsr_temporal_tta_select_final_test_predictions.csv'); pa=pd.read_csv('outputs/audio_feature_benchmarks/audio_pairwise_contact_stress_cv_select/reports/audio_pairwise_contact_stress_cv_select_final_test_predictions.csv'); assert np.array_equal(hi.audio_file.astype(str),pa.audio_file.astype(str)); base=sl.normalize(.8*hi[sl.PROBA_COLUMNS].to_numpy()+.2*pa[sl.PROBA_COLUMNS].to_numpy()); segp,w2s=sp.segment_proba_from_window(hi,base); codes=sp.specimen_codes_for_segments(hi,w2s); locked=json.load(open('outputs/audio_feature_benchmarks/audio_specimen_contact_lift_select/reports/audio_specimen_contact_lift_select_selected_without_test.json'))['selected_without_test']; segp=sl.consensus_and_lift(segp,codes,float(locked['consensus_threshold']),int(locked['min_contact_segments']),locked['lift_min_mass'],float(locked['lift_floor']),float(locked['lift_confidence'])); p=segp[w2s]; pc,lg=avr.avr_inputs(p); tk=seg(tf.audio_file).to_numpy(); su,sc=np.unique(tk,return_inverse=True); tsi=np.array([np.where(tu==specimen(pd.Series([k])).iloc[0])[0][0] for k in su]); tpc=np.array([pc[sc==i].mean() for i in range(len(su))]); tmat=np.vstack([suite.normalize(np.exp(lg[sc==i].mean(0,keepdims=True)))[0] for i in range(len(su))]); taudio=np.vstack([suite.normalize(tmat[tsi==i].mean(0,keepdims=True))[0] for i in range(len(tu))]); im=model.predict_proba(tx); mat=suite.normalize((1-iw)*taudio+iw*im); predseg=np.where(tpc>=th,mat[tsi].argmax(1)+1,0); pred=predseg[sc]; by=(y>0).astype(int); bp=(pred>0).astype(int); result={'split':'robot_test_final','protocol':lock['protocol'],'locked_candidate':b,'n':len(y),'accuracy_4class':accuracy_score(y,pred),'macro_precision_4class':precision_score(y,pred,average='macro',zero_division=0),'macro_recall_4class':recall_score(y,pred,average='macro',zero_division=0),'macro_f1_4class':f1_score(y,pred,average='macro',zero_division=0),'weighted_f1_4class':f1_score(y,pred,average='weighted',zero_division=0),'binary_macro_f1':f1_score(by,bp,average='macro',zero_division=0),'per_class_4class':classification_report(y,pred,labels=[0,1,2,3],target_names=['ambient','leaf','trunk','twig'],output_dict=True,zero_division=0),'confusion_matrix_4class':confusion_matrix(y,pred,labels=[0,1,2,3]).tolist(),'no_test_leakage':True}; (OUT/'spec_lift_visual_final_test_metrics.json').write_text(json.dumps(result,indent=2,default=float)); print(json.dumps(result,indent=2,default=float))
if __name__=='__main__': main()
